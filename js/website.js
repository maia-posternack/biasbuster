//makes pellet object
function storeWebsite(url, title, adj, sort, pair, auth, authurl) {
  var site = {

    URL: url,
    TITLE: title,
    ADJ: adj,
    SORT: sort,
    PAIR: pair,
    AUTH: auth,
    AUTHURL: authurl,

  }
  return site;
}
